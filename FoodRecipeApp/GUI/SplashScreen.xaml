<mah:MetroWindow x:Class="FoodRecipeApp.GUI.SplashScreen"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:li="clr-namespace:LoadingIndicators.WPF;assembly=LoadingIndicators.WPF"
                 xmlns:local="clr-namespace:FoodRecipeApp"
                 xmlns:wpf="clr-namespace:LoadingIndicators.WPF;assembly=LoadingIndicators.WPF"
                 Title="FOOD RECIPE APP" 
                 Height="450" 
                 Width="800"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
                 ResizeMode="CanResizeWithGrip"
                 WindowStartupLocation="CenterScreen"
				 WindowState="Maximized"
				 Icon="/Resources/recipe-book.ico"
                 BorderThickness="1"
				 Loaded="MainWindow_OnLoaded"
				 Unloaded="MainWindow_OnUnloaded"
                 mc:Ignorable="d">

	<Grid>
		<li:LoadingIndicator Grid.Column="0" Grid.Row="0" SpeedRatio="{Binding SpeedRatio}" IsActive="{Binding IsArcsActive}" Style="{DynamicResource LoadingIndicatorArcsRingStyleKey}" Foreground="DeepPink" />
		<!--<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
			<TextBlock FontSize="32"
			           FontWeight="Light"
			           Text="Welcome to the MahApps.Metro World..." />
			<Border Height="5" Background="{DynamicResource MahApps.Brushes.Accent}" />

			--><!--<mah:MetroProgressBar x:Name="TheProgressBar" Minimum="0" Maximum="1000" Value="300" Height="25" Margin="0 10" />
			<ProgressBar x:Name="TheOtherProgressBar" Minimum="0" Maximum="1000" Value="600" Height="25" Margin="0 5" />--><!--
		</StackPanel>-->
	</Grid>

	<mah:MetroWindow.Resources>
		<Style x:Key="CustomLightMinButtonStyle"
               BasedOn="{StaticResource MahApps.Styles.Button.MetroWindow.Light}"
               TargetType="{x:Type Button}">
			<Setter Property="Background" Value="{StaticResource MahApps.Brushes.Accent}" />
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="DarkGreen" />
					<Setter Property="Foreground" Value="White" />
				</Trigger>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background" Value="Green" />
				</Trigger>
			</Style.Triggers>
		</Style>
		<Style x:Key="CustomDarkMinButtonStyle"
               BasedOn="{StaticResource MahApps.Styles.Button.MetroWindow.Dark}"
               TargetType="{x:Type Button}">
			<Setter Property="Background" Value="{StaticResource MahApps.Brushes.Accent}" />
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="DarkGreen" />
					<Setter Property="Foreground" Value="White" />
				</Trigger>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background" Value="Green" />
				</Trigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="CustomLightMaxButtonStyle"
               BasedOn="{StaticResource MahApps.Styles.Button.MetroWindow.Light}"
               TargetType="{x:Type Button}">
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="DarkGoldenrod" />
					<Setter Property="Foreground" Value="White" />
				</Trigger>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background" Value="Yellow" />
					<Setter Property="Foreground" Value="Black" />
				</Trigger>
			</Style.Triggers>
		</Style>
		<Style x:Key="CustomDarkMaxButtonStyle"
               BasedOn="{StaticResource MahApps.Styles.Button.MetroWindow.Dark}"
               TargetType="{x:Type Button}">
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="DarkGoldenrod" />
					<Setter Property="Foreground" Value="White" />
				</Trigger>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background" Value="Yellow" />
					<Setter Property="Foreground" Value="Black" />
				</Trigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="CustomLightCloseButtonStyle"
               BasedOn="{StaticResource MahApps.Styles.Button.MetroWindow.Close.Light.Win10}"
               TargetType="{x:Type Button}">
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="DarkRed" />
					<Setter Property="Foreground" Value="White" />
				</Trigger>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background" Value="Red" />
				</Trigger>
			</Style.Triggers>
		</Style>
		<Style x:Key="CustomDarkCloseButtonStyle"
               BasedOn="{StaticResource MahApps.Styles.Button.MetroWindow.Close.Dark.Win10}"
               TargetType="{x:Type Button}">
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="DarkRed" />
					<Setter Property="Foreground" Value="White" />
				</Trigger>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background" Value="Red" />
				</Trigger>
			</Style.Triggers>
		</Style>

	</mah:MetroWindow.Resources>

	<mah:MetroWindow.WindowButtonCommands>
		<mah:WindowButtonCommands Minimize="A custom Tooltip which will be shown on Min button"
                                       Maximize="Maximize the window"
                                       Restore="Restore the window"
                                       Close="Noooo... don't close me!"
                                       LightMinButtonStyle="{StaticResource CustomLightMinButtonStyle}"
                                       LightMaxButtonStyle="{StaticResource CustomLightMaxButtonStyle}"
                                       LightCloseButtonStyle="{StaticResource CustomLightCloseButtonStyle}"
                                       DarkMinButtonStyle="{StaticResource CustomDarkMinButtonStyle}"
                                       DarkMaxButtonStyle="{StaticResource CustomDarkMaxButtonStyle}"
                                       DarkCloseButtonStyle="{StaticResource CustomDarkCloseButtonStyle}" />
	</mah:MetroWindow.WindowButtonCommands>

	<mah:MetroWindow.LeftWindowCommands>
		<mah:WindowCommands>
			<Button Click="LaunchGitHubSite" ToolTip="Open up the GitHub site">
				<iconPacks:PackIconModern Width="22"
				                          Height="22"
				                          Kind="SocialGithubOctocat" />
			</Button>
		</mah:WindowCommands>
	</mah:MetroWindow.LeftWindowCommands>

	<!--<mah:MetroWindow.RightWindowCommands>
		<mah:WindowCommands>
			<Button Click="DeployCupCakes" Content="Deploy CupCakes">
				<Button.ContentTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal">
							<iconPacks:PackIconModern Width="22"
							                          Height="22"
							                          VerticalAlignment="Center"
							                          Kind="FoodCupcake" />
							<TextBlock Margin="4 0 0 0"
							           VerticalAlignment="Center"
							           Text="{Binding}" />
						</StackPanel>
					</DataTemplate>
				</Button.ContentTemplate>
			</Button>
		</mah:WindowCommands>
	</mah:MetroWindow.RightWindowCommands>-->
	<mah:MetroWindow.RightWindowCommands>
		<mah:WindowCommands>
			<Button Click="ChangeAppStyleButtonClick">
				<StackPanel Orientation="Horizontal">
					<iconPacks:PackIconModern Width="24" Height="24" Kind="FoodCupcake" />
					<TextBlock Margin="4 0 0 0" VerticalAlignment="Center" Text="Change App Style..." />
				</StackPanel>
			</Button>
		</mah:WindowCommands>
	</mah:MetroWindow.RightWindowCommands>

	
</mah:MetroWindow>
